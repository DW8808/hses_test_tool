# 雲端同步設定教學

這一步是把「借還/庫存」資料放到一份 Google 試算表當共用資料庫，這樣不管誰、在哪台裝置、哪個網路，只要能上網，看到的都是同一份資料。

**完全免費，不需要信用卡。** 用的是 Google 個人帳號的 Sheets（試算表）和 Apps Script（Google 提供的免費小型雲端程式），沒有任何付費方案牽涉在內。

這幾個步驟需要登入**你自己的** Google 帳號操作（不需要是學校帳號），這部分我沒辦法幫你代勞——不是技術限制，是 Google 的登入畫面本來就只認得你自己的帳號密碼。但整個流程只有「貼上一段程式碼、按幾個按鈕」，跟著做就可以，卡住的話把畫面截圖貼給我，我可以繼續帶你排除問題。

## 步驟 1：建立 Google 試算表

前往 [sheets.google.com](https://sheets.google.com) → 建立空白試算表 → 隨便命名（例如「測驗工具庫存資料庫」）。

## 步驟 2：貼上程式碼

1. 上方選單點 **擴充功能 → Apps Script**，會開一個新分頁。
2. 裡面預設有一個空的 `Code.gs`，把裡面內容全部刪掉。
3. 打開這個專案裡的 `apps-script/Code.gs`，全選、複製，貼到剛剛清空的地方。
4. 按 Ctrl/Cmd + S 存檔（會要你幫這個專案命名，隨便取都可以）。

## 步驟 3：執行兩個設定按鈕（只需要各按一次）

1. 先在程式碼裡找到 `setPin` 函式，把 `var pin = '請改成一組只有你自己知道的密碼';` 改成你自己的密碼（**不要用這份教學、聊天紀錄、或任何公開地方出現過的字串**，因為這個 `Code.gs` 檔案會放在公開的 GitHub repo 裡，裡面本來就不該有真正的密碼）。
2. 存檔後，上方工具列的函式下拉選單選 **setPin**，按旁邊的「▶ 執行」。
   - 第一次執行 Google 會跳出授權畫面：選帳號 →（如果看到「Google 尚未驗證這個應用程式」）點「進階」→「前往...（不安全）」→「允許」。這是正常的，因為這是你自己貼上的程式，Google 只是還沒幫它做官方審核而已，不影響安全性。
2. 函式下拉選單改選 **setupSheets**，按「▶ 執行」。
   - 執行完回到試算表分頁，應該會看到多了 `Items`、`History`、`Borrowers` 三個分頁，`Items` 裡已經有目前所有測驗工具資料。

## 步驟 4：部署成網址

1. 右上角「部署」→「新增部署作業」。
2. 齒輪圖示選類型「網頁應用程式」。
3. 「執行身分」選**我**，「誰可以存取」選**所有人**。
4. 點「部署」，再授權一次（同步驟 3）。
5. 完成後會給你一個網址，長得像：
   `https://script.google.com/macros/s/AKfycb.../exec`
   複製起來。

> 之後如果改了程式碼想更新，要「部署」→「管理部署作業」→ 編輯（鉛筆）→ 版本選「新版本」→ 部署，網址才會生效，單純存檔不會自動套用。

## 步驟 5：把網址寫進 config.js

打開專案裡的 `config.js`，把 `url` 的值換成步驟 4 拿到的網址：

```js
window.GAS_CONFIG = {
  url: '你的 Apps Script 網址（.../exec 結尾）',
};
```

存檔後，如果是本機使用直接重新整理網頁即可；如果已經部署到 GitHub Pages，要把這個檔案的變更 `git add` / `git commit` / `git push` 上去才會生效。

這個網址本身不是密碼，寫在公開的 `config.js` 裡沒關係——真正擋住陌生人的是 PIN，PIN 永遠不會出現在任何檔案或連結裡。

## 步驟 6：給老師的連結就是網站本身

給其他老師的連結**不需要加任何參數**，就是網站首頁本身，例如：

```
https://dw8808.github.io/hses_test_tool/
```

老師第一次打開會自動跳出一個小視窗：上面顯示 Web App 網址（灰底鎖住、不能編輯），下面是 PIN 輸入框。你另外口頭或紙條告訴他們 PIN，他們輸入一次、按「確認」連線後，這台裝置以後就會自動記住，不用再輸入第二次。

這個畫面**沒有**「取消連線」按鈕，老師沒辦法誤按到中斷連線或改到別的網址。

如果你（管理者）想要看到完整的「⚙️ 連線設定」（可以編輯網址、PIN、或取消連線退回本機模式），網址後面加 `?admin=1`，例如：

```
https://dw8808.github.io/hses_test_tool/?admin=1
```

## 常見問題

- **忘記 PIN？** 回 Apps Script 編輯器，把 `setPin` 裡的密碼改新的、重新執行一次即可覆蓋。
- **想離線用？** 用 `?admin=1` 打開連線設定，按「取消連線」即可退回純本機模式，跟雲端資料庫互不影響、不會遺失雲端資料。
- **未來 Excel 清冊有更新？** 把新 Excel 換進資料夾、執行 `npm run parse` 再執行 `npm run seed`，把更新後的 `apps-script/Code.gs` 內容整份重新貼回 Apps Script 編輯器，重新部署（步驟4的「新版本」），再執行一次 `setupSheets`。
- **安全性？** 只要這組網址和 PIN 沒有外流就不會被亂改，但這不是銀行等級的保護，請不要放真正機密的資料。**切記：真正的 PIN 只能存在 Apps Script 的 Script Properties 裡（透過 `setPin` 設定），絕對不要把它寫進任何會進 GitHub 的檔案。**
